if SERVER
if PLAYER
SUBDIRS = lib common player server util doc
else
SUBDIRS = lib common server util doc
endif
else
if PLAYER
SUBDIRS = lib common player util doc
else
SUBDIRS = lib util doc
endif
endif
