if SERVER
if PLAYER
SUBDIRS = lib common player server util
else
SUBDIRS = lib common player server util
endif
else
if PLAYER
SUBDIRS = lib common player util
else
SUBDIRS = lib common util
endif
endif
